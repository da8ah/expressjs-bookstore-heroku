<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{#each headers}}
    <{{header.tag}} {{header.property}}="{{header.value}}"></{{header.tag}}>
    {{/each}}
    <title>{{title}}</title>
</head>

<body>
    <div class="container">
        {{{ body }}}
    </div>
    {{#each scripts}}
    <script src="{{script}}"></script>
    {{/each}}
</body>

</html>